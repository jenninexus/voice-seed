# Code-simplify / code-review notes — voice SSOTs (2026-07-14)

## Verdict

**Moderate** cleanup applied. Layering was already correct; main issues were
registry drift, stale Shade public catchphrase, and Patreon draft voice
hardcoded away from format-manifests.

## Findings addressed

| Tag | What | Action |
|---|---|---|
| [DUPE] | Shade public card ≠ live AI-primary catchphrase | Updated `characters/humans/shade.md` |
| [DUPE] | `patreon-draft.ts` BRAND_VOICE ≠ manifest `patreon_format.voice` | Script reads per-brand manifest |
| [BLOAT] | Profile `roleTracks.available[]` copy of vault | Slimmed Shade profile to pointer |
| [BLOAT] | `Self-Described.md` post-assimilation full prose | Stub → vault `#mission` |
| [BLOAT] | `identity.background` restating characterVoice | Pointer line on both users |
| [--] | Registry missing `mission`, studio dual profiles | Added to `registry.json` + REGISTERS |
| [--] | Root/socials example missing `voiceSsot` | Added networkMap pointers |
| [OK] | `storage/brands/*.json` | Color-only — left alone |
| [OK] | Vault `voice.vsPartner` kept | Intentional for `/make-resume` without leaving vault |

## Follow-up same day — bot / Agency protocol

- Added `botGreeter` register + `bot-jenni` / `bot-martian` registry entries
- Hub: `docs/PROTOCOL.md` + `docs/ROADMAP.md`
- `/voice-design bot …` + two-layer Agency edit path documented
- Theme kits explicitly non-voice (color only)

## Left alone (intentional)

- Vault ↔ characterVoice short personality overlap — make-resume needs vault self-contained
- `hardFacts` on users — agent convenience summaries
- `refrence/` typo directory — pointers already use it
- Agency agent WIP (chatVoice on agents/*.md) — other agent’s open edits
- Root `format-manifest.json` full format rollup — still useful for multi-brand tools; now documents voice lives per-brand

## Machine compatibility

- Private vaults stay machine-local (`storage/` is gitignored). Public cards use relative paths only.
