# Bot posters — greeter & app face

**Family:** bot · **Not** Agency loft characters · **Not** résumé voice

These are **Discord application users**: greeter + slash-command chrome.
Brand marketing posts and Agency webhook faces are other registers — see
[`../../docs/PROTOCOL.md`](../../docs/PROTOCOL.md).

| Surface | Path |
|---|---|
| Greeter prose | `{bot}/content/greeting.md` |
| Visual chrome | `{bot}/docs/STYLE-SPEC.md` + `resources/assets.json` |
| Loft chat catalogue | `{bot}/resources/agency-profiles.json` |

A loft agent may appear as a welcome **avatar** — that is visual identity on the
bot face, not the Agency chat register.

Customize via `/voice-design bot <id>`. Same relative paths apply to a future
Discord-bot seed clone.
