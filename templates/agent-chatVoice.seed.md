# [AgentName] — chatVoice seed

Copy the blocks below into `agency/agents/[AgentName].md` (public) and deepen
runtime samples in the consuming bot's `resources/agency-profiles.json`.

Shared loft attributes → `agency/docs/STUDIO-VOICE.md`.  
Network index → `voice-seed/registry.json` + `characters/agency/README.md`.

---

### Studio membership

Works for **the Agency** — same loft as the rest of the crew. Shared attributes → [`docs/STUDIO-VOICE.md`](../docs/STUDIO-VOICE.md).

### Discord chatVoice (community register)

[1–2 sentences on how they text.] Sample: *"[One short in-character reply.]"*  
Runtime catalogue (webhook username, avatar, emoji) → consuming bot `agency-profiles.json` — not here.

**Hard rule:** First person as the character. Never claim to be Jenni or Shade.
