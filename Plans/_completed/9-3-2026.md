# Voice Seed public-seed leak scrub

Session handoff for 2026-09-03. **Plans/** is the session log.

Register: **map / seeds** (not a prose register). Hub sequence §2 — bare-minimum public
slice so Agency clone users can optionally add this map.

## Done when

- [x] `registry.json` uses relative `ssotPattern` only
- [x] `docs/PROTOCOL.md` Flow C uses relative agency / bot paths
- [x] README is clone-first, contrast-safe badges, LICENSE, tip footer
- [x] Public tree has no machine-absolute disks, no legal surname, no personal mailbox, no studio operator cards
- [x] Handshake / investor / collab cards are **not** in this repo (Handshake hub `Voice/` only)
- [x] Git **history** rewritten to a single clean snapshot (see `9-3-2026-public-history.md`)
- [x] GitHub visibility → Public (2026-09-03)

## Task checklist

- [x] Handshake cards restored into Handshake hub `Voice/` from git history, then kept off this tree
- [x] `.gitignore` belts: `**/*handshake*` · `**/*-investor*` · `**/*-collab*`
- [x] Public `/voice-design` has no handshake / investor / collab subcommands
- [x] Agency pairing docs written locally (GETTING-STARTED, STUDIO-VOICE, README) — **not pushed**
- [x] Product card associations + JN store draft in product-design `docs/VOICE-SEED.md`
- [x] Workspace README style protocol updated (local workspace root, not this clone)

## Assumptions (ratified)

1. Handshake / investor / collab stay in the Handshake hub, never on public GitHub, not even as examples.
2. `Plans/` is tracked and clone-safe.
3. Visibility flip waits on **history**, not only HEAD.

## Evidence

| Claim | Result |
|-------|--------|
| Relative `ssotPattern` | VERIFIED |
| Handshake cards off tracked tree | VERIFIED `git ls-files` empty for handshake |
| HEAD leak scan | VERIFIED empty for drive-letter / surname / mailbox |
| Git history still leaks | SUPERSEDED — orphan snapshot on `origin/main` (see `9-3-2026-public-history.md`) |
| GitHub still private | SUPERSEDED — public 2026-09-03 |
| Tip footer + LICENSE | VERIFIED |

## Next-agent prompts (per repo)

History rewrite and visibility: `9-3-2026-public-history.md` (same `_completed/` folder). Remaining work is **not** in this tree (Agency pairing push, product-design registry cell).
